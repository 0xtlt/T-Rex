/* eslint-disable import/prefer-default-export */
type ConfigTRex = {
  installed: boolean;
  infos: {
    blogName: string;
    blogDescription: string;
  };
  theme: string;
};

export type { ConfigTRex };
